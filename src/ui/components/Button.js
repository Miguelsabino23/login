export default function Button() {
  return (
    <button
      style={{
        backgroundColor: "#0078F0",
        color: "#ffffff",
        border: "none",
        width: "100%",
        padding: ".8rem 0",
        fontSize: "1rem",
        borderRadius: ".2rem",
      }}
    >
      ENTRAR
    </button>
  );
}
